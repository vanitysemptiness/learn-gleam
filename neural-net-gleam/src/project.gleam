import gleam/io

pub fn main() {
  io.println("Hello from project!")
  todo as "use the xor dataset for the NN module"
  todo as "use the mnist for an image dataset"
  todo as "iris dataset if you want something really simple"
}
